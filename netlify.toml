[build]
  command = "npm run build"
  publish = "dist"

[dev]
  framework = "#custom"
  command = "vite --host"
  targetPort = 5173
  port = 8888
  publish = "dist"
  startTimeout = 60

[functions]
  node_bundler = "esbuild"

[functions."*"]
  timeout = 60